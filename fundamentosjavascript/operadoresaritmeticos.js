//atribuição aditiva, subtrativa, multiplicatica, divisiva, modular, 
//operadores são aritimétricos, de atribuição, operadores relacionais, operadores lógicos, classicado tbm pela quantidade de operandos 
//operados são binários, operam em cima de dois operandos
const [a, b, c, d,] = [3, 5, 1, 15] 
const soma = a + b + c + d //operação binária uma soma, sintaxe conhecida como infix

//d++ //operando e operador, operador chamado postfixed, fica depois do operando, 
//++d //pré fix, antes do operando, 
//op1 + op2 //operador operando uma sintaxe infix 
//-a //operado uniário em cima de um operando
const subtracao = d - a
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)

//binário, pq exitem dois operandos